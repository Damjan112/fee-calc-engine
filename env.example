# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_password
DB_DATABASE=fee_calc

# Database Synchronization (set to false for production)
DB_SYNCHRONIZE=false

# Database Logging (set to false for production)
DB_LOGGING=true

# Application Port
PORT=3000 